export enum VoteType {
    SingleChoice = 0,
    MultiChoice = 1
}
export enum VoteStatus {
    Open = 0,
    Closed = 1
}
export enum AnnouncementPriority {
    Normal = 0,
    Important = 1,
    Urgent = 2
}
